
# ── OS ──────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
.trash

# ── IDE / Editor ────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~
.project
# Ignore local Claude Code state (settings, worktrees, private skills) …
.claude/*
!.claude/skills/
.claude/skills/*
# … except the skills we deliberately share publicly with contributors:
!.claude/skills/README.md
!.claude/skills/cmp-integration/
!.claude/skills/config-builder/
!.claude/skills/integration-check/
!.claude/skills/live-inspector/
.settings/
*.sublime-project
*.sublime-workspace
.kateproject.*
*.kate-swp
.obsidian

# ── Secrets ──────────────────────────────────────────────────
.env
.runner

# ── Internal working protocol (German, maintainer-local; ADR-0002) ──
CLAUDE.local.md

# ── Node.js ─────────────────────────────────────────────────
node_modules/

# ── Logs & temp ─────────────────────────────────────────────
*.log
*.tmp
*.bak
logs/
tmp/

# ── Internal docs (API specs, integration notes, smoketests) ────────────────
# Reference material for maintainers; not part of the public release.
internal/

# ── Internal working docs (German, maintainer-local; ADR-0002) ──────────────
# Decision records, findings, QA checklists, glossary, open decisions and the
# curated knowledge base are the project's internal working protocol — same
# "intern = Deutsch" class as CLAUDE.local.md. Kept local, not published.
# (aboard-sync.json at the repo root stays versioned on purpose — public roadmap.)
docs/
knowledge/

# ── aBoard (ADR-0049 / ADR-0067) ────────────────────────────
# aboard-sync.json IS versioned on purpose (public project roadmap).
# Everything below is local runtime/build state and stays out of git.
aboard.html
aboard.template.html
aboard-sync.base.json
aboard-sync.base.*.json
aboard-sync.inbox/
aboard-sync.chat-tail.*
aboard.html.bak-*

# aGTM Inspector: derived ZIP artifact (reproduce with scripts/pack-devtools-extension.sh)
/aGTM-Inspector.zip
