[test]
# Only test/ — tmp/ is a scratch area and may hold throwaway *.test.js files
# that would otherwise be collected and inflate the count.
root = "./test"
preload = ["./test/setup.js"]
timeout = 10000
