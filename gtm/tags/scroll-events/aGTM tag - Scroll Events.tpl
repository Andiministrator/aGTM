___TERMS_OF_SERVICE___

By creating or modifying this file you agree to Google Tag Manager's Community
Template Gallery Developer Terms of Service available at
https://developers.google.com/tag-manager/gallery-tos (or such other URL as
Google may provide), as modified from time to time.


___INFO___

{
  "type": "TAG",
  "id": "cvt_temp_public_id",
  "version": 1,
  "securityGroups": [],
  "displayName": "aGTM - Scroll Events",
  "brand": {
    "id": "brand_dummy",
    "displayName": "aGTM",
    "thumbnail": "data:image/png;base64,"
  },
  "description": "A better scroll tracking.\nRequires an aGTM integration of the GTM.",
  "containerContexts": [
    "WEB"
  ]
}


___TEMPLATE_PARAMETERS___

[
  {
    "type": "TEXT",
    "name": "eventname",
    "displayName": "Event Name",
    "simpleValueType": true,
    "valueValidators": [
      {
        "type": "NON_EMPTY"
      }
    ],
    "defaultValue": "scroll"
  },
  {
    "type": "SIMPLE_TABLE",
    "name": "addparameter",
    "displayName": "Additional Event Parameter",
    "simpleTableColumns": [
      {
        "defaultValue": "",
        "displayName": "Name",
        "name": "pkey",
        "type": "TEXT"
      },
      {
        "defaultValue": "",
        "displayName": "Value",
        "name": "pvalue",
        "type": "TEXT"
      }
    ],
    "help": "\u003cstrong\u003eAttention!\u003c/strong\u003e The parameters are set at the time the tag is triggered, not when the actual text copy event occurs."
  },
  {
    "type": "CHECKBOX",
    "name": "ua_event",
    "checkboxText": "Add event_category, event_action and event_label",
    "simpleValueType": true
  },
  {
    "type": "TEXT",
    "name": "steps",
    "displayName": "Scroll Steps",
    "simpleValueType": true,
    "valueValidators": [
      {
        "type": "NON_EMPTY"
      },
      {
        "type": "REGEX",
        "args": [
          "\\d+[0-9,]*"
        ],
        "errorMessage": "The value has to be a comma-separated list of Integers"
      }
    ],
    "defaultValue": "25,50,75,90"
  },
  {
    "type": "CHECKBOX",
    "name": "noscrollevent",
    "checkboxText": "Send an event, if viewport is to small to scroll",
    "simpleValueType": true
  },
  {
    "type": "CHECKBOX",
    "name": "isscrollevent",
    "checkboxText": "Send an Event, if the User has scrolled (1x per page)",
    "simpleValueType": true
  }
]


___SANDBOXED_JS_FOR_WEB_TEMPLATE___

// Import needed libraries
const JSON = require('JSON');
const callInWindow = require('callInWindow');
const copyFromWindow = require('copyFromWindow');
const setInWindow = require('setInWindow');
const Math = require('Math');

/**
 * Build ND aGTM shadow object
 * @version 1.1
 * @lastupdate 13.07.2026 by Andi Petzoldt <andi@petzoldt.net>
 * @author Andi Petzoldt <andi@petzoldt.net>
 * @property {object} o
 * @param {object} c - config
 *   @param {object} c.debug - debug messages in browser console
 * @param {object} d - data object
 *   @param {object} d.d - a copy of the sandbox data object
 *   @param {object} d.e - the current event object
 *   @param {object} d.q - event queue
 * @param {object} f - function library
 */
var o = o || { c: {debug:false}, d:{ d:data, e:{}, q:[] }, f:{} };

// Get tag configuration
o.c.eventname = data.eventname;
o.c.stepstring = typeof data.steps=='string' ? data.steps : '25,50,75,90';
o.c.addparameter = typeof data.addparameter=='object' ? data.addparameter : [];
o.c.noscrollevent = typeof data.noscrollevent=='boolean' ? data.noscrollevent : false;
o.c.isscrollevent = typeof data.isscrollevent=='boolean' ? data.isscrollevent : false;
o.c.ua_event = typeof data.ua_event=='boolean' ? data.ua_event : false;

// Initiate variables
// Parse the step thresholds: trim, coerce to number, drop empty/NaN/out-of-range
// and duplicates, then sort ascending. Without this an out-of-order or
// trailing-comma config (e.g. "50,25," ) leaves a threshold unreachable (the
// while-loop below advances monotonically) or emits a garbage ">%" event.
o.d.steps = [];
o.c.stepstring.split(',').forEach(function(s) {
  var n = Math.round(s * 1);
  if (typeof n=='number' && n>0 && n<=100 && o.d.steps.indexOf(n)===-1) o.d.steps.push(n);
});
o.d.steps.sort(function(a, b) { return a - b; });
if (typeof o.d.scrolldepth_max!='number') o.d.scrolldepth_max = 0;
if (typeof o.d.scrolldepth_next!='number') o.d.scrolldepth_next = 0;
if (typeof o.d.viewport_width!='number') o.d.viewport_width = 0;
if (typeof o.d.viewport_height!='number') o.d.viewport_height = 0;
if (typeof o.d.viewport_relation!='number') o.d.viewport_relation = 1;
o.d.viewport_start = 1.5;
if (typeof o.d.document_width!='number') o.d.document_width = 0;
if (typeof o.d.document_height!='number') o.d.document_height = 0;
o.d.hasscrolled = false;
o.d.e.event = o.c.eventname;

/**
 * Define function to get the sizes of the document and the viewport
 * Usage: o.f.getSizes();
 */
o.f.getSizes = o.f.getSizes || function() {
  o.d.viewport_width=callInWindow('aGTM.f.getVal','w','innerWidth');
  o.d.viewport_height=callInWindow('aGTM.f.getVal','w','innerHeight');
  var offsetWidth=callInWindow('aGTM.f.getVal','b','offsetWidth');
  var offsetHeight=callInWindow('aGTM.f.getVal','b','offsetHeight');
  var clientWidth=callInWindow('aGTM.f.getVal','b','clientWidth');
  var clientHeight=callInWindow('aGTM.f.getVal','b','clientHeight');
  var scrollWidth=callInWindow('aGTM.f.getVal','b','scrollWidth');
  var scrollHeight=callInWindow('aGTM.f.getVal','b','scrollHeight');
  if(typeof offsetHeight=='number') {
    o.d.document_width = offsetWidth;
    o.d.document_height = offsetHeight;
  }
  if(typeof clientHeight=='number' && clientHeight>o.d.document_height) {
    o.d.document_width = clientWidth;
    o.d.document_height = clientHeight;
  }
  if(typeof scrollHeight=='number' && scrollHeight>o.d.document_height) {
    o.d.document_width = scrollWidth;
    o.d.document_height = scrollHeight;
  }
  if(o.d.viewport_height>0){
    o.d.viewport_relation = o.d.document_height / o.d.viewport_height;
  }
};

/**
 * Define function to get the current scroll depth
 * @returns {number} - the current scroll depth as Integer
 * Usage: o.f.getScrollDepth();
 */
o.f.getScrollDepth = o.f.getScrollDepth || function() {
  o.f.getSizes();
  var scrollPos = callInWindow('aGTM.f.getVal','w','scrollY') || callInWindow('aGTM.f.getVal','w','scrollTop') || callInWindow('aGTM.f.getVal','s','s') || 0;
  scrollPos+= o.d.viewport_height;
  if (o.d.document_height<=0 || scrollPos<=0) return 0;
  var scrollDepth = Math.round(100 / (o.d.document_height / scrollPos));
  //if (scrollDepth>scrolldepth_max) scrolldepth_max = scrollDepth;
  return scrollDepth;
};

/**
 * Define function to get the maximum scroll depth and send an event, if it has increased
 * Usage: o.f.getMaxScrollDepth();
 */
o.f.getMaxScrollDepth = o.f.getMaxScrollDepth || function() {
  if (o.d.viewport_relation>o.d.viewport_start && o.d.scrolldepth_next<o.d.steps.length) {
    // Prepare event
    o.c.addparameter.forEach(function(row) {
      o.d.e[row.pkey] = row.pvalue;
    });
    var scrolldepth = o.f.getScrollDepth();
    if (scrolldepth>o.d.scrolldepth_max) {
      o.d.scrolldepth_max = scrolldepth;
      if (o.c.isscrollevent && !o.d.hasscrolled) {
        o.d.hasscrolled = true;
        var ev = JSON.parse(JSON.stringify(o.d.e));
        ev.event = 'scrolled';
        ev.event_label = 'User has scrolled';
        if (o.c.ua_event) {
          ev.event_category = 'User Experience';
          ev.event_action = ev.event;
        }
        callInWindow('aGTM.f.fire', ev);
        o.d.q.push(ev);
      }
    }
    while (o.d.scrolldepth_max>=o.d.steps[o.d.scrolldepth_next]) {
      var e = JSON.parse(JSON.stringify(o.d.e));
      e.event_label = '>'+o.d.steps[o.d.scrolldepth_next].toString()+'%';
      e.scroll_depth = o.d.steps[o.d.scrolldepth_next];
      e.scrolldepth = o.d.steps[o.d.scrolldepth_next] * 1;
      e.viewport_relation = Math.round(o.d.viewport_relation);
      if (o.c.ua_event) {
        e.event_category = 'User Experience';
        e.event_action = e.event;
      }
      callInWindow('aGTM.f.fire', e);
      o.d.q.push(e);
      o.d.scrolldepth_next++;
    }
  }
};

// Run
o.f.getSizes();
if (o.d.viewport_relation>o.d.viewport_start) {
  // Register the scroll/resize listeners at most once per page. A multi-trigger
  // or SPA setup can execute this tag repeatedly; without the guard each run adds
  // another listener (never removed) whose closure keeps firing → duplicated
  // scroll events + a listener leak. Note: on SPA route changes the first
  // listener is reused (scroll depth is not reset per virtual page) — run only
  // one scroll-tracking tag per page.
  if (!copyFromWindow('aGTM.d.scrollListener_active')) {
    setInWindow('aGTM.d.scrollListener_active', true, true);
    // CWV: passive listeners never block scrolling (better INP/no scroll jank);
    // the throttle bounds how often the handlers' layout reads (getSizes:
    // offsetHeight/scrollHeight/…) force a reflow. resize 250ms, scroll 200ms.
    callInWindow('aGTM.f.evLstn','window','resize',o.f.getSizes,{passive:true,throttle:250});
    callInWindow('aGTM.f.evLstn','window','scroll',o.f.getMaxScrollDepth,{passive:true,throttle:200});
  }
} else if (o.c.noscrollevent) {
  // Prepare event
  o.c.addparameter.forEach(function(row) {
    o.d.e[row.pkey] = row.pvalue;
  });
  var e = JSON.parse(JSON.stringify(o.d.e));
  e.event_label = 'scroll tracking not necessary';
  e.viewport_relation = Math.round(o.d.viewport_relation);
  if (o.c.ua_event) {
    e.event_category = 'User Experience';
    e.event_action = e.event;
  }
  callInWindow('aGTM.f.fire', e);
  o.d.q.push(e);
}

// Call data.gtmOnSuccess when the tag is finished.
data.gtmOnSuccess();


___WEB_PERMISSIONS___

[
  {
    "instance": {
      "key": {
        "publicId": "access_globals",
        "versionId": "1"
      },
      "param": [
        {
          "key": "keys",
          "value": {
            "type": 2,
            "listItem": [
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.f.fire"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.d.scrollListener_active"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.f.getVal"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.f.evLstn"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              }
            ]
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  }
]


___TESTS___

scenarios:
- name: Steps are parsed trimmed deduped range-checked and sorted ascending
  code: |-
    // F-34c: an out-of-order / dup / out-of-range config must be normalised, else
    // the monotonic while-loop stops at the first unreachable threshold and later
    // steps never fire. Driving a full scroll must emit 25,50,75,90 in order.
    let depths = [], scrollHandler = null;
    mock('callInWindow', function(fn) {
      if (fn === 'aGTM.f.evLstn') { if (arguments[2] === 'scroll') scrollHandler = arguments[3]; return; }
      if (fn === 'aGTM.f.getVal') {
        let t = arguments[1], p = arguments[2];
        if (t === 'w' && p === 'innerHeight') return 1000;
        if (t === 'w' && p === 'innerWidth') return 1000;
        if (t === 'w' && p === 'scrollY') return 2000;
        if (t === 'b' && p === 'offsetHeight') return 3000;
        if (t === 'b' && p === 'offsetWidth') return 1000;
        return 0;
      }
      if (fn === 'aGTM.f.fire') { if (typeof arguments[1].scroll_depth === 'number') depths.push(arguments[1].scroll_depth); return; }
    });
    mock('copyFromWindow', function() { return false; });
    mock('setInWindow', function() {});
    runCode({ eventname: 'scroll', steps: '75,25,50,50,110,0,90', addparameter: [], noscrollevent: false, isscrollevent: false, ua_event: false });
    scrollHandler();
    assertThat(depths.length).isEqualTo(4);
    assertThat(depths[0]).isEqualTo(25);
    assertThat(depths[1]).isEqualTo(50);
    assertThat(depths[2]).isEqualTo(75);
    assertThat(depths[3]).isEqualTo(90);
- name: Scroll listeners are registered at most once per page
  code: |-
    // F-34b: leak guard. When aGTM.d.scrollListener_active is already set, the tag
    // must NOT register the scroll/resize listeners again (else a listener leak).
    let evLstnCalls = 0;
    mock('callInWindow', function(fn) {
      if (fn === 'aGTM.f.evLstn') { evLstnCalls++; return; }
      if (fn === 'aGTM.f.getVal') {
        let t = arguments[1], p = arguments[2];
        if (t === 'w' && p === 'innerHeight') return 1000;
        if (t === 'b' && p === 'offsetHeight') return 3000;
        return 0;
      }
      if (fn === 'aGTM.f.fire') { return; }
    });
    mock('copyFromWindow', function(key) { if (key === 'aGTM.d.scrollListener_active') return true; return false; });
    mock('setInWindow', function() {});
    runCode({ eventname: 'scroll', steps: '25,50', addparameter: [], noscrollevent: false, isscrollevent: false, ua_event: false });
    assertThat(evLstnCalls).isEqualTo(0);
- name: Short page with noscrollevent fires the not-necessary event
  code: |-
    let fired = null;
    mock('callInWindow', function(fn) {
      if (fn === 'aGTM.f.evLstn') { return; }
      if (fn === 'aGTM.f.getVal') {
        let t = arguments[1], p = arguments[2];
        if (t === 'w' && p === 'innerHeight') return 1000;
        if (t === 'b' && p === 'offsetHeight') return 1000;
        return 0;
      }
      if (fn === 'aGTM.f.fire') { fired = arguments[1]; return; }
    });
    mock('copyFromWindow', function() { return false; });
    mock('setInWindow', function() {});
    runCode({ eventname: 'scroll', steps: '25,50', addparameter: [], noscrollevent: true, isscrollevent: false, ua_event: false });
    assertThat(fired).isDefined();
    assertThat(fired.event_label).isEqualTo('scroll tracking not necessary');
- name: Additional parameters are merged into the scroll event
  code: |-
    let fired = null, scrollHandler = null;
    mock('callInWindow', function(fn) {
      if (fn === 'aGTM.f.evLstn') { if (arguments[2] === 'scroll') scrollHandler = arguments[3]; return; }
      if (fn === 'aGTM.f.getVal') {
        let t = arguments[1], p = arguments[2];
        if (t === 'w' && p === 'innerHeight') return 1000;
        if (t === 'w' && p === 'scrollY') return 2000;
        if (t === 'b' && p === 'offsetHeight') return 3000;
        return 0;
      }
      if (fn === 'aGTM.f.fire') { if (typeof arguments[1].scroll_depth === 'number') fired = arguments[1]; return; }
    });
    mock('copyFromWindow', function() { return false; });
    mock('setInWindow', function() {});
    runCode({ eventname: 'scroll', steps: '25', addparameter: [{ pkey: 'foo', pvalue: 'bar' }], noscrollevent: false, isscrollevent: false, ua_event: false });
    scrollHandler();
    assertThat(fired).isDefined();
    assertThat(fired.foo).isEqualTo('bar');
setup: ''


___NOTES___

# aGTM Custom Template

- Version 1.1
- Autor: Andi Petzoldt <andi@petzoldt.net>
- Last Update: 13.07.2026

## Description

The GTM scroll tracking sends events even if the user hasn't scrolled.
This Custom Template is for a better scroll tracking.
Requires an aGTM integration of the GTM (library v1.5+).

## Fixes in 1.1

- Core Web Vitals: the scroll/resize listeners are now registered passive
  (never block scrolling → better INP/no jank) and throttled (scroll 200 ms,
  resize 250 ms) via the new `aGTM.f.evLstn` options — the layout reads in
  `getSizes()` no longer run on every scroll event. Requires aGTM library v1.5+.
- Registration guard: the listeners are added at most once per page, so a
  multi-trigger/SPA setup no longer stacks duplicate listeners (event
  multiplication + leak). Run only one scroll-tracking tag per page; on SPA
  route changes the first listener is reused (scroll depth is not reset per
  virtual page).
- The `steps` config is trimmed, numeric-coerced, de-duplicated and sorted
  ascending, so an out-of-order or trailing-comma value (e.g. `"50,25,"`) can no
  longer make a threshold unreachable or emit a stray `>%` event. `scroll_depth`
  is now a number.
- Cleanup: dropped dead `require`s (`logToConsole`, `queryPermission`), the
  unused `logging` permission and the over-broad `aGTM` read/write global
  (least-privilege: only `execute` on the concrete `aGTM.f.*` paths plus a
  granular `aGTM.d.scrollListener_active` read/write remain).


