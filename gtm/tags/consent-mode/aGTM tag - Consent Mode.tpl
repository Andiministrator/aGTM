___TERMS_OF_SERVICE___

By creating or modifying this file you agree to Google Tag Manager's Community
Template Gallery Developer Terms of Service available at
https://developers.google.com/tag-manager/gallery-tos (or such other URL as
Google may provide), as modified from time to time.


___INFO___

{
  "type": "TAG",
  "id": "cvt_temp_public_id",
  "__wm": "VGVtcGFsdGUtQXV0aG9yX0NvbnNlbnRNb2RlLVNpbW8tQWhhdmE\u003d",
  "version": 1,
  "securityGroups": [],
  "displayName": "aGTM - Consent Mode",
  "categories": [
    "UTILITY",
    "ANALYTICS",
    "ADVERTISING"
  ],
  "brand": {
    "id": "github.com_gtm-templates-simo-ahava",
    "displayName": "aGTM",
    "thumbnail": "data:image/png;base64,"
  },
  "description": "Set the Google (and optionally Microsoft) Consent Mode signals according to the aGTM consent.",
  "containerContexts": [
    "WEB"
  ]
}


___TEMPLATE_PARAMETERS___

[
  {
    "type": "CHECKBOX",
    "name": "debug",
    "checkboxText": "Debug Mode",
    "simpleValueType": true
  },
  {
    "type": "CHECKBOX",
    "name": "cm_update",
    "checkboxText": "Use it as Consent Update (instead of Default)",
    "simpleValueType": true,
    "enablingConditions": [
      {
        "paramName": "cm_update_after_default",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "CHECKBOX",
    "name": "cm_update_after_default",
    "checkboxText": "Fire a Consent Mode Update Event after a Consent Mode Default",
    "simpleValueType": true,
    "help": "First it fires a Consent Mode Default Event with all flags denied. After that a Consent Mode Update Event will fire with the correct flags.",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "GROUP",
    "name": "cm_conditions",
    "displayName": "Consent Mode Conditions",
    "groupStyle": "NO_ZIPPY",
    "subParams": [
      {
        "type": "PARAM_TABLE",
        "name": "cm_attributes",
        "displayName": "Consent Attribute",
        "paramTableColumns": [
          {
            "param": {
              "type": "SELECT",
              "name": "cm_attribute",
              "displayName": "Consent Mode Attributes",
              "macrosInSelect": true,
              "selectItems": [
                {
                  "value": "ad_storage",
                  "displayValue": "ad_storage"
                },
                {
                  "value": "ad_user_data",
                  "displayValue": "ad_user_data"
                },
                {
                  "value": "ad_personalization",
                  "displayValue": "ad_personalization"
                },
                {
                  "value": "analytics_storage",
                  "displayValue": "analytics_storage"
                },
                {
                  "value": "personalization_storage",
                  "displayValue": "personalization_storage"
                },
                {
                  "value": "functionality_storage",
                  "displayValue": "functionality_storage"
                },
                {
                  "value": "security_storage",
                  "displayValue": "security_storage"
                }
              ],
              "simpleValueType": true
            },
            "isUnique": false
          },
          {
            "param": {
              "type": "SELECT",
              "name": "cm_type",
              "displayName": "Consent Type",
              "macrosInSelect": true,
              "selectItems": [
                {
                  "value": "purposes",
                  "displayValue": "Purpose"
                },
                {
                  "value": "purposeIDs",
                  "displayValue": "Purpose ID"
                },
                {
                  "value": "services",
                  "displayValue": "Service"
                },
                {
                  "value": "serviceIDs",
                  "displayValue": "Service ID"
                },
                {
                  "value": "vendors",
                  "displayValue": "Vendor"
                },
                {
                  "value": "vendorIDs",
                  "displayValue": "Vendor ID"
                }
              ],
              "simpleValueType": true
            },
            "isUnique": false
          },
          {
            "param": {
              "type": "TEXT",
              "name": "cm_value",
              "displayName": "Consent Value",
              "simpleValueType": true,
              "valueValidators": [
                {
                  "type": "NON_EMPTY"
                }
              ]
            },
            "isUnique": false
          }
        ]
      }
    ],
    "help": "Add each consent mode attribute that should be set and define the conditions for it. There can be multiple conditions for each attribute.\n\u003cbr /\u003e\u003cbr /\u003e\nOnly if all defined conditions apply will the attribute be set to \"granted\". If one of the conditions is not true, the attribute is set to \"denied\".\n\u003cbr /\u003e\u003cbr /\u003e\nIf a consent mode attribute is not defined here, it will not be sent unless something else is defined for it in the Consent Default Settings.\n\u003cbr /\u003e\u003cbr /\u003e\nExample of \"ad_user_data\" which should be set to \"granted\" if the \"Google Ads\" service has been agreed to:\u003cbr /\u003e\n\u003cul\u003e\n\u003cli\u003eConsent Mode Attribute: ad_user_data\u003c/li\u003e\n\u003cli\u003eConsent Type: Service\u003c/li\u003e\n\u003cli\u003eConsent Value: ,Google Ads,\u003c/li\u003e\n\u003c/ul\u003e"
  },
  {
    "type": "TEXT",
    "name": "cm_wait",
    "displayName": "Wait for Consent Update",
    "simpleValueType": true,
    "valueUnit": "milliseconds",
    "defaultValue": 0,
    "valueValidators": [
      {
        "type": "NON_NEGATIVE_NUMBER"
      }
    ],
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "TEXT",
    "name": "cm_regions",
    "displayName": "Regions",
    "simpleValueType": true,
    "defaultValue": "all",
    "help": "Set the regions according to \u003ca href\u003d\"https://en.wikipedia.org/wiki/ISO_3166-2\"\u003eISO_3166-2\u003c/a\u003e (comma-separated). Select \u003cstrong\u003eall\u003c/strong\u003e to apply the consent mode for all users.",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ],
    "valueValidators": [
      {
        "type": "NON_EMPTY",
        "errorMessage": "Set to \"all\" for all regions or add a comma-separated list of regions."
      }
    ]
  },
  {
    "type": "LABEL",
    "name": "eu_regions",
    "displayName": "\u003cb\u003eEEA Countries:\u003c/b\u003e\u003cbr /\u003eAT,BE,BG,CY,CZ,DE,DK,EE,ES,FI,FR,GR,HR,HU,IE,IS,IT,LI,LT,LU,LV,MT,NL,NO,PL,PT,RO,SE,SI,SK,CH,GB",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "CHECKBOX",
    "name": "cm_grant_outside",
    "checkboxText": "Grant consent by default outside the configured regions",
    "simpleValueType": true,
    "help": "Only relevant when Regions is set to specific regions (not <strong>all</strong>). Emits an additional GLOBAL granted default (Google's two-default pattern: a region-scoped default plus a global granted fallback), so visitors OUTSIDE the configured regions are granted by default instead of being left unset. Use this when you suppress the consent banner outside those regions and want everything allowed there. The region-scoped default is more specific and still wins inside the regions.",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "LABEL",
    "name": "further_options",
    "displayName": "\u003cbr /\u003e\u003cb\u003eFurther Options:\u003c/b\u003e\u003cbr /\u003e"
  },
  {
    "type": "CHECKBOX",
    "name": "url_passthrough",
    "checkboxText": "Pass through URL parameters",
    "simpleValueType": true,
    "help": "When a visitor lands on an advertiser\u0027s website after clicking an ad, information about the ad might be appended to the landing page URLs as a query parameter. To improve conversion accuracy, Google tags usually store this information in first-party cookies on the advertiser\u0027s domain.\n\u003cbr /\u003e\u003cbr /\u003e\nHowever, if \u003cstrong\u003ead_storage\u003c/strong\u003e is \u003cstrong\u003edenied\u003c/strong\u003e, Google tags won\u0027t save this information locally. To improve ad click measurement quality in this case, advertisers can optionally pass ad click information through URL parameters across pages using a feature called URL passthrough.\n\u003cbr /\u003e\u003cbr /\u003e\nSimilarly, if \u003cstrong\u003eanalytics_storage\u003c/strong\u003e is set to denied, URL passthrough can be used to send event and session-based analytics (including conversions) without cookies across pages.\n\u003cbr /\u003e\u003cbr /\u003e\nThe following conditions must be met to use URL passthrough:\n\u003cul\u003e\n\u003cli\u003eConsent-aware Google tags are present on the page.\u003c/li\u003e\n\u003cli\u003eThe site has opted in to using the URL passthrough feature.\u003c/li\u003e\n\u003cli\u003eConsent Mode is implemented on the page.\u003c/li\u003e\n\u003cli\u003eThe outgoing link refers to the same domain as the current page\u0027s domain.\u003c/li\u003e\n\u003cli\u003eA gclid/dclid is present in the URL (Google Ads and Floodlight tags only)\u003c/li\u003e\n\u003c/ul\u003e",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "CHECKBOX",
    "name": "ads_data_redaction",
    "checkboxText": "Redact Ads Data",
    "simpleValueType": true,
    "help": "When \u003cstrong\u003ead_storage\u003c/strong\u003e is denied, no new cookies are set for advertising purposes. Additionally, third-party cookies previously set on google.com and doubleclick.net won\u0027t be used. Data sent to Google will still include the full page URL, including any ad click information in the URL parameters.\n\u003cbr /\u003e\u003cbr /\u003e\nTo further redact your ads data when \u003cstrong\u003ead_storage\u003c/strong\u003e is denied, set \u003cstrong\u003eads_data_redaction\u003c/strong\u003e to true.\n\u003cbr /\u003e\u003cbr /\u003e\nWhen \u003cstrong\u003eads_data_redaction\u003c/strong\u003e is true and \u003cstrong\u003ead_storage\u003c/strong\u003e is denied, ad click identifiers sent in network requests by Google Ads and Floodlight tags will be redacted.",
    "enablingConditions": [
      {
        "paramName": "cm_update",
        "paramValue": true,
        "type": "NOT_EQUALS"
      }
    ]
  },
  {
    "type": "CHECKBOX",
    "name": "ms_consent_mode",
    "checkboxText": "Fire Microsoft Consent Mode",
    "simpleValueType": true,
    "help": "Also send the consent state to Microsoft UET (ad_storage, ad_user_data, ad_personalization) and Microsoft Clarity (ad_Storage, analytics_Storage). Signals left at \"not set\" are NOT sent - and Microsoft has no unset state, so a signal it never receives counts as GRANTED. If you switch this on, set the advertising signals explicitly."
  },
  {
    "type": "CHECKBOX",
    "name": "cm_event",
    "checkboxText": "DataLayer Event",
    "simpleValueType": true,
    "help": "Fire a separate dataLayer event with Consent Mode signals"
  },
  {
    "type": "GROUP",
    "name": "cm_defaults",
    "displayName": "Consent Default Settings",
    "groupStyle": "ZIPPY_OPEN_ON_PARAM",
    "subParams": [
      {
        "type": "SELECT",
        "name": "ad_storage",
        "displayName": "ad_storage",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "ad_user_data",
        "displayName": "ad_user_data",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "ad_personalization",
        "displayName": "ad_personalization",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "analytics_storage",
        "displayName": "analytics_storage",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "personalization_storage",
        "displayName": "personalization_storage",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "functionality_storage",
        "displayName": "functionality_storage",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      },
      {
        "type": "SELECT",
        "name": "security_storage",
        "displayName": "security_storage",
        "macrosInSelect": true,
        "selectItems": [
          {
            "value": "not_set",
            "displayValue": "not set"
          },
          {
            "value": "denied",
            "displayValue": "denied"
          },
          {
            "value": "granted",
            "displayValue": "granted"
          }
        ],
        "simpleValueType": true,
        "defaultValue": "not_set"
      }
    ]
  }
]


___SANDBOXED_JS_FOR_WEB_TEMPLATE___

// Import needed libraries
const log = require('logToConsole');
const JSON = require('JSON');
const Object = require('Object');
const copyFromWindow = require('copyFromWindow');
const setInWindow = require('setInWindow');
const callInWindow = require('callInWindow');
const gtagSet = require('gtagSet');
const makeNumber = require('makeNumber');
const createQueue = require('createQueue');
const setDefaultConsentState = require('setDefaultConsentState');
const updateConsentState = require('updateConsentState');

// Config
const debug = typeof data.debug=='boolean' ? data.debug : false;

/**
 * Build aGTM shadow object
 * @property {object} o
 * @param {object} c - config
 * @param {object} c.debug - debug messages in browser console
 * @param {object} d - data object
 * @param {object} d.f - Events that have been fired before GTM has loaded
 * @param {object} d.q - event queue
 * @param {object} f - function library
 */
var o = o || { c: {debug:false}, d:{}, f:{} };
o.c.cm_signals = [
  'ad_storage',
  'ad_user_data',
  'ad_personalization',
  'analytics_storage',
  'personalization_storage',
  'functionality_storage',
  'security_storage'
];

// Get tag configuration
if (debug) log('info','Tag Configuration',data);
o.c.cm_update = typeof data.cm_update=='boolean' ? data.cm_update : false;
o.c.cm_update_after_default = typeof data.cm_update_after_default=='boolean' ? data.cm_update_after_default : false;
o.c.cm_attributes = data.cm_attributes || [];
o.c.cm_wait = typeof data.cm_wait=='string' ? makeNumber(data.cm_wait) : 0;
o.c.cm_regions = typeof data.cm_regions=='string' ? data.cm_regions : 'all';
o.c.url_passthrough = typeof data.url_passthrough=='boolean' ? data.url_passthrough : false;
o.c.ads_data_redaction = typeof data.ads_data_redaction=='boolean' ? data.ads_data_redaction : false;
o.c.ms_consent_mode = typeof data.ms_consent_mode=='boolean' ? data.ms_consent_mode : false;
o.c.cm_event = typeof data.cm_event=='boolean' ? data.cm_event : false;
o.c.cm_grant_outside = typeof data.cm_grant_outside=='boolean' ? data.cm_grant_outside : false;

// Get consent signals from aGTM. Guard against aGTM not being loaded yet
// (copyFromWindow returns undefined): without this, the .hasResponse access
// below throws a TypeError, so setDefaultConsentState/updateConsentState never
// run and NO deny-default is set - fail-open exactly in the consent-critical
// moment (e.g. when the tag fires on the Consent Initialization trigger before
// aGTM populated aGTM.d.consent). Fail closed: missing consent = {}.
var consent = copyFromWindow('aGTM.d.consent') || {};
if (debug) log('info','aGTM Consent Info', JSON.parse(JSON.stringify(consent)));

// Run Consent Check Fallback: when aGTM has no valid consent response yet, ask
// the CMP directly, THEN re-read the freshly written consent so the signal
// computation below actually uses it (the copy taken above is stale and would
// otherwise ignore what consent_check just wrote). No debug gating - the
// fallback must run in production too.
if (typeof consent.hasResponse!='boolean' || !consent.hasResponse) {
  const c_check = callInWindow('aGTM.f.consent_check', 'init');
  if (debug) log('info','consent_check Info', c_check);
  consent = copyFromWindow('aGTM.d.consent') || consent;
}

// Helper function to check a defined consent in aGTM
o.f.chk_cons = function (consKey, consVal) {
  if (typeof consent[consKey]!='string') return false;
  return consent[consKey].indexOf(consVal)>=0 ? true : false;
};

// Program
o.d.cm = {};
for (var i=0; i<o.c.cm_signals.length; i++) {
  var s = o.c.cm_signals[i];
  var c = '';
  if (o.c.cm_attributes.length>0) {
    for (var j=0; j<o.c.cm_attributes.length; j++) {
      var a = o.c.cm_attributes[j];
      if (a.cm_attribute==s && c!='denied') {
        c = o.f.chk_cons(a.cm_type,a.cm_value) ? 'granted' : 'denied';
      }
    }
  }
  if (!c && data[s]=='not_set') continue;
  o.d.cm[s] = c ? c : data[s];
}
if (!o.c.cm_update && o.c.cm_wait>0 && !o.c.cm_update_after_default) o.d.cm.wait_for_update = o.c.cm_wait;
if (!o.c.cm_update && o.c.cm_regions!='all' && o.c.cm_regions) {
  o.d.cm.region = o.c.cm_regions.split(',').map(r => r.trim());
  o.d.cm.region = o.d.cm.region.filter((val, idx) => o.d.cm.region.indexOf(val) === idx);
  if (debug) log('info','CM Regions', o.d.cm.region);
}

// Fire Consent Mode dataLayer Event
if (o.c.cm_event) {
  const c_ev = { event:'aGTM_consent_mode', cm_signals:o.d.cm };
  // Uses aGTM.f.fire rather than a raw dataLayer push, so the signal event lands in
  // aGTM's event log and goes out through sendnaus() with its dataLayer-hook protection.
  // Note it is NOT consent-gated or queued: fire() lets any event whose name starts with
  // "aGTM" past both, which is what this signal needs — it has to reach GTM before the
  // consent decision. That is also why the template needs no access_globals grant on the
  // global dataLayer (gtagSet's writes are covered by write_data_layer instead).
  callInWindow('aGTM.f.fire', c_ev);
}

// Other settings
if (!o.c.cm_update) {
  gtagSet({
    url_passthrough: o.c.url_passthrough,
    ads_data_redaction: o.c.ads_data_redaction
  });
}

// Set the consent state
if (o.c.cm_update) {
  updateConsentState(o.d.cm);
  if (debug) log('info','Consent Update', JSON.parse(JSON.stringify(o.d.cm)));
} else {
  // Optional GLOBAL granted fallback (Google's two-default pattern): when a
  // specific region is configured (o.d.cm.region present) and cm_grant_outside
  // is on, emit a global granted default (no region) FIRST so visitors OUTSIDE
  // the configured regions are granted by default instead of being left unset
  // (Consent Mode treats a region-scoped-only default as undefined elsewhere).
  // The region-scoped default set below is more specific and wins inside the
  // regions. Without this, "everything allowed outside the regions" relied
  // solely on the update firing granted in time.
  if (o.c.cm_grant_outside && o.d.cm.region) {
    o.d.cmg = {};
    // Grant ONLY the signals the region-scoped default below actually covers
    // (i.e. those present in o.d.cm). Signals left at "not_set" are excluded
    // from o.d.cm (see the `not_set` skip above), so granting all 7 here would
    // make Consent Mode's per-signal merge fall those not_set signals back to
    // this global granted value INSIDE the deny region too - a fail-open. By
    // mirroring o.d.cm's signal set, every signal the regional default covers
    // wins in-region, and not_set signals stay unset everywhere.
    for (var gi=0; gi<o.c.cm_signals.length; gi++) {
      var gsig = o.c.cm_signals[gi];
      if (typeof o.d.cm[gsig] != 'undefined') o.d.cmg[gsig] = 'granted';
    }
    setDefaultConsentState(o.d.cmg);
    if (debug) log('info','Consent Global Granted Default (outside regions)', JSON.parse(JSON.stringify(o.d.cmg)));
  }
  if (o.c.cm_update_after_default) {
    o.d.cmd = {};
    for (var i=0; i<o.c.cm_signals.length; i++) {
      var s = o.c.cm_signals[i];
      o.d.cmd[s] = 'denied';
    }
    // Region belongs on the DEFAULT: `region` is only valid on
    // setDefaultConsentState, not updateConsentState (the update is already
    // location-independent). Without this the all-denied default applied
    // GLOBALLY - a configured cm_regions was silently ignored, so the deny
    // baseline hit visitors outside the intended regions too (F-31c). Scoping
    // the denied default to cm_regions means the region-less update below sets
    // the real consent per user everywhere else. NOTE: outside the region there
    // is now no aGTM default (Consent Mode treats unset per Google's rules); the
    // region-less updateConsentState provides the actual state for those users.
    if (o.d.cm.region) o.d.cmd.region = o.d.cm.region;
    setDefaultConsentState(o.d.cmd);
    if (debug) log('info','Consent Default before Update', 'Default', JSON.parse(JSON.stringify(o.d.cmd)), 'Update', JSON.parse(JSON.stringify(o.d.cm)));
    let cm = JSON.parse(JSON.stringify(o.d.cm));
    Object.delete(cm, 'wait_for_update');
    Object.delete(cm, 'region'); // correct: updateConsentState has no region param
    updateConsentState(cm);
    if (debug) log('info','Consent Update after Default', JSON.parse(JSON.stringify(cm)));
  } else {
    setDefaultConsentState(o.d.cm);
    if (debug) log('info','Consent Default', JSON.parse(JSON.stringify(o.d.cm)));
  }
}

// Fire Microsoft Consent Mode
// Microsoft is a separate consent channel with its own vocabulary, not a mirror
// of Google's - and a push to it is not free. Measured in the live
// bat.bing.com/bat.js (2026-08-19): the FIRST consent push sets
// uetConfig.consent.enabled = true, and from then on shouldEnforce() returns
// false. That switches off two mechanisms UET runs by itself:
//   - its own enforcement in the EEA, the UK and Switzerland, where UET's
//     default is DENIED (elsewhere it is granted), and
//   - preEnforce(), which reads the Google Consent Mode state out of
//     window.google_tag_data.ics and subscribes to TCF about 1.5 s after load -
//     i.e. it would pick up what the Google half of THIS tag just set.
// Two rules follow, and they are the reason this branch looks the way it does.
if (o.c.ms_consent_mode) {
  // Rule 1: only 'granted' and 'denied' count as configured. The signal SELECTs
  // accept a GTM variable (macrosInSelect), so the value is whatever it
  // resolved to at runtime - an empty string from an unset variable must not
  // read as a state. In bat.js adStorageAllowed is computed as
  // ('denied' !== ad_storage) starting from an initial TRUE, so ANY value that
  // is not the literal 'denied' lands on granted.
  o.f.ms_val = function (v) {
    return (v === 'granted' || v === 'denied') ? v : '';
  };
  o.d.ms = {};
  var msSignals = ['ad_storage', 'ad_user_data', 'ad_personalization'];
  for (var mi=0; mi<msSignals.length; mi++) {
    var msv = o.f.ms_val(o.d.cm[msSignals[mi]]);
    if (msv) o.d.ms[msSignals[mi]] = msv;
  }
  // Rule 2: never push without ad_storage. It is the only signal UET evaluates
  // (ad_user_data and ad_personalization do not appear in bat.js at all), so a
  // payload without it would disable the enforcement above AND leave the
  // visitor on granted - the worst of both. When ad_storage is not configured
  // here, sending NOTHING is the fail-closed branch, not the fail-open one:
  // UET then keeps its own default, which is denied and enforced in the
  // EEA/UK/CH, and can still adopt the Google Consent Mode state.
  if (o.d.ms.ad_storage) {
    const uetq = createQueue('uetq');
    if (o.c.cm_update) {
      uetq('consent', 'update', o.d.ms);
      if (debug) log('info','Microsoft UET Consent Update', JSON.parse(JSON.stringify(o.d.ms)));
    } else if (o.c.cm_update_after_default) {
      // A baseline is a baseline: all three denied, regardless of what the
      // update will carry. Building it from the signals that happen to be set
      // could leave ad_storage out of the very default the mode exists for.
      // UET has no region parameter - the Microsoft baseline is global, only
      // the Google default is region-scoped.
      o.d.msd = {};
      for (var mj=0; mj<msSignals.length; mj++) o.d.msd[msSignals[mj]] = 'denied';
      uetq('consent', 'default', o.d.msd);
      uetq('consent', 'update', o.d.ms);
      if (debug) log('info','Microsoft UET Consent', 'Default', JSON.parse(JSON.stringify(o.d.msd)), 'Update', JSON.parse(JSON.stringify(o.d.ms)));
    } else {
      // UET understands wait_for_update as well (bat.js caps it at 10000 ms).
      // The Google branch has already computed it under exactly this condition;
      // without passing it on, UET would fire hits carrying the default state
      // during the window the integrator asked it to wait.
      if (o.d.cm.wait_for_update) o.d.ms.wait_for_update = o.d.cm.wait_for_update;
      uetq('consent', 'default', o.d.ms);
      if (debug) log('info','Microsoft UET Consent Default', JSON.parse(JSON.stringify(o.d.ms)));
    }
  } else if (debug) {
    log('info','Microsoft UET Consent skipped - ad_storage is not configured, so UET keeps its own default and its own enforcement');
  }
  // Clarity is all-or-nothing: Microsoft documents BOTH consentv2 fields as
  // required. Sending only one would be a call missing a required field, and
  // filling the other one in would deny a signal the integrator never
  // configured - Clarity has its own project-level consent setting to fall back
  // on. Note the ad_Storage / analytics_Storage casing is Microsoft's own.
  var clrAd = o.f.ms_val(o.d.cm.ad_storage);
  var clrAn = o.f.ms_val(o.d.cm.analytics_storage);
  if (clrAd && clrAn) {
    const clarityFct = () => {
      const clarity = copyFromWindow('clarity');
      if (clarity) return clarity;
      setInWindow('clarity', function() { callInWindow('clarity.q.push', arguments); });
      createQueue('clarity.q');
      return copyFromWindow('clarity');
    };
    const clarity = clarityFct();
    o.d.clr = { ad_Storage: clrAd, analytics_Storage: clrAn };
    clarity('consentv2', o.d.clr);
    if (debug) log('info','Microsoft Clarity Consent', JSON.parse(JSON.stringify(o.d.clr)));
  } else if (debug) {
    log('info','Microsoft Clarity Consent skipped - consentv2 needs both ad_Storage and analytics_Storage');
  }
}

// Set aGTM.d.cm
setInWindow('aGTM.d.cm', JSON.parse(JSON.stringify(o.d.cm)), true);

// Call data.gtmOnSuccess when the tag is finished.
data.gtmOnSuccess();


___WEB_PERMISSIONS___

[
  {
    "instance": {
      "key": {
        "publicId": "logging",
        "versionId": "1"
      },
      "param": [
        {
          "key": "environments",
          "value": {
            "type": 1,
            "string": "debug"
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  },
  {
    "instance": {
      "key": {
        "publicId": "access_globals",
        "versionId": "1"
      },
      "param": [
        {
          "key": "keys",
          "value": {
            "type": 2,
            "listItem": [
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.d.consent"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.d.cm"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "uetq"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.f.fire"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "aGTM.f.consent_check"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "clarity"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "clarity.q"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": false
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "key"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  },
                  {
                    "type": 1,
                    "string": "execute"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "clarity.q.push"
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": false
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              }
            ]
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  },
  {
    "instance": {
      "key": {
        "publicId": "access_consent",
        "versionId": "1"
      },
      "param": [
        {
          "key": "consentTypes",
          "value": {
            "type": 2,
            "listItem": [
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "ad_storage"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "analytics_storage"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "personalization_storage"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "functionality_storage"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "security_storage"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "ad_user_data"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              },
              {
                "type": 3,
                "mapKey": [
                  {
                    "type": 1,
                    "string": "consentType"
                  },
                  {
                    "type": 1,
                    "string": "read"
                  },
                  {
                    "type": 1,
                    "string": "write"
                  }
                ],
                "mapValue": [
                  {
                    "type": 1,
                    "string": "ad_personalization"
                  },
                  {
                    "type": 8,
                    "boolean": true
                  },
                  {
                    "type": 8,
                    "boolean": true
                  }
                ]
              }
            ]
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  },
  {
    "instance": {
      "key": {
        "publicId": "write_data_layer",
        "versionId": "1"
      },
      "param": [
        {
          "key": "keyPatterns",
          "value": {
            "type": 2,
            "listItem": [
              {
                "type": 1,
                "string": "url_passthrough"
              },
              {
                "type": 1,
                "string": "ads_data_redaction"
              }
            ]
          }
        }
      ]
    },
    "clientAnnotations": {
      "isEditedByUser": true
    },
    "isRequired": true
  }
]


___TESTS___

scenarios:
- name: Unresolved aGTM consent still sets a denied default - fail-open guard
  code: |-
    // aGTM not loaded yet: copyFromWindow('aGTM.d.consent') returns undefined.
    // Without the `|| {}` guard the consent.hasResponse access throws and
    // setDefaultConsentState never runs (fail-open). It must still fire denied.
    let defArg = null;
    mock('copyFromWindow', function() { return undefined; });
    mock('callInWindow', function() { return undefined; });
    mock('setDefaultConsentState', function(o) { defArg = o; });
    runCode({
      ad_storage: 'denied', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'denied', personalization_storage: 'denied',
      functionality_storage: 'denied', security_storage: 'denied'
    });
    assertThat(defArg).isDefined();
    assertThat(defArg.ad_storage).isEqualTo('denied');
- name: Granted signals pass through to the default consent state
  code: |-
    let defArg = null;
    mock('copyFromWindow', function() { return { hasResponse: true }; });
    mock('setDefaultConsentState', function(o) { defArg = o; });
    runCode({
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'granted',
      functionality_storage: 'granted', security_storage: 'granted'
    });
    assertThat(defArg).isDefined();
    assertThat(defArg.ad_storage).isEqualTo('granted');
    assertThat(defArg.analytics_storage).isEqualTo('granted');
    assertApi('updateConsentState').wasNotCalled();
- name: Update mode calls updateConsentState instead of the default
  code: |-
    let updArg = null;
    mock('copyFromWindow', function() { return { hasResponse: true }; });
    mock('updateConsentState', function(o) { updArg = o; });
    runCode({
      cm_update: true,
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'granted',
      functionality_storage: 'granted', security_storage: 'granted'
    });
    assertThat(updArg).isDefined();
    assertThat(updArg.ad_storage).isEqualTo('granted');
    assertApi('setDefaultConsentState').wasNotCalled();
- name: Update after default scopes the denied default to the configured region
  code: |-
    // F-31c: `region` belongs on setDefaultConsentState (the denied baseline),
    // NOT on updateConsentState (which has no region param). The all-denied
    // default must carry the region; the region-less update sets real consent.
    let defArg = null, updArg = null;
    mock('copyFromWindow', function() { return { hasResponse: true }; });
    mock('setDefaultConsentState', function(o) { defArg = o; });
    mock('updateConsentState', function(o) { updArg = o; });
    runCode({
      cm_update_after_default: true,
      cm_regions: 'DE,AT',
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'granted',
      functionality_storage: 'granted', security_storage: 'granted'
    });
    assertThat(defArg).isDefined();
    assertThat(defArg.ad_storage).isEqualTo('denied');
    assertThat(defArg.region).isDefined();
    assertThat(defArg.region.length).isEqualTo(2);
    assertThat(defArg.region[0]).isEqualTo('DE');
    assertThat(defArg.region[1]).isEqualTo('AT');
    assertThat(updArg).isDefined();
    assertThat(updArg.ad_storage).isEqualTo('granted');
    assertThat(updArg.region).isUndefined();
- name: Consent-check fallback re-reads the freshly written consent
  code: |-
    // The tag re-reads aGTM.d.consent AFTER consent_check so the signal
    // computation uses what the CMP just wrote. Without the re-read the stale
    // (empty) first copy is used and the attribute maps to denied.
    let n = 0, defArg = null;
    mock('copyFromWindow', function(key) {
      if (key === 'aGTM.d.consent') {
        n++;
        return n === 1 ? { hasResponse: false, purposes: '' }
                       : { hasResponse: true, purposes: 'analytics' };
      }
      return undefined;
    });
    mock('callInWindow', function() { return undefined; });
    mock('setDefaultConsentState', function(o) { defArg = o; });
    runCode({
      cm_attributes: [{ cm_attribute: 'analytics_storage', cm_type: 'purposes', cm_value: 'analytics' }],
      ad_storage: 'not_set', ad_user_data: 'not_set', ad_personalization: 'not_set',
      analytics_storage: 'not_set', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(defArg).isDefined();
    assertThat(defArg.analytics_storage).isEqualTo('granted');
- name: not_set signals are omitted from the consent state
  code: |-
    let defArg = null;
    mock('copyFromWindow', function() { return { hasResponse: true }; });
    mock('setDefaultConsentState', function(o) { defArg = o; });
    runCode({
      ad_storage: 'not_set', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'granted', personalization_storage: 'denied',
      functionality_storage: 'denied', security_storage: 'denied'
    });
    assertThat(defArg).isDefined();
    assertThat(defArg.ad_storage).isUndefined();
    assertThat(defArg.analytics_storage).isEqualTo('granted');
- name: Microsoft UET never receives a blank signal for a not_set attribute
  code: |-
    // Microsoft treats a signal it never receives as GRANTED, so pushing
    // {ad_storage: undefined} would fail open. With every signal at not_set
    // neither the uetq queue nor Clarity may be touched at all.
    let uetCalls = [];
    let clarityCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) { clarityCalls.push([a, b]); };
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'not_set', ad_user_data: 'not_set', ad_personalization: 'not_set',
      analytics_storage: 'not_set', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(0);
    assertThat(clarityCalls.length).isEqualTo(0);
- name: Microsoft gets all three UET ad signals plus the Clarity pair
  code: |-
    let uetCalls = [];
    let clarityCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) { clarityCalls.push([a, b]); };
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'denied',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(1);
    assertThat(uetCalls[0][0]).isEqualTo('uetq');
    assertThat(uetCalls[0][1]).isEqualTo('consent');
    assertThat(uetCalls[0][2]).isEqualTo('default');
    assertThat(uetCalls[0][3].ad_storage).isEqualTo('granted');
    assertThat(uetCalls[0][3].ad_user_data).isEqualTo('granted');
    assertThat(uetCalls[0][3].ad_personalization).isEqualTo('denied');
    // UET has no analytics_storage - it must not leak into the UET payload.
    assertThat(uetCalls[0][3].analytics_storage).isUndefined();
    assertThat(clarityCalls.length).isEqualTo(1);
    assertThat(clarityCalls[0][0]).isEqualTo('consentv2');
    assertThat(clarityCalls[0][1].ad_Storage).isEqualTo('granted');
    assertThat(clarityCalls[0][1].analytics_Storage).isEqualTo('granted');
- name: Update after default sends UET a denied baseline before the real state
  code: |-
    // The mode used to be derived from cm_update alone, so UET received a
    // single default carrying the real consent and never saw the baseline.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('updateConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true, cm_update_after_default: true,
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(2);
    assertThat(uetCalls[0][2]).isEqualTo('default');
    assertThat(uetCalls[0][3].ad_storage).isEqualTo('denied');
    assertThat(uetCalls[0][3].ad_personalization).isEqualTo('denied');
    assertThat(uetCalls[1][2]).isEqualTo('update');
    assertThat(uetCalls[1][3].ad_storage).isEqualTo('granted');
- name: Update mode sends UET exactly one update and no analytics_storage
  code: |-
    // This branch was free to mutate: swapping the mode or leaking the whole
    // o.d.cm into the payload kept every scenario green until this one existed.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('updateConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true, cm_update: true,
      ad_storage: 'granted', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(1);
    assertThat(uetCalls[0][2]).isEqualTo('update');
    assertThat(uetCalls[0][3].ad_storage).isEqualTo('granted');
    assertThat(uetCalls[0][3].analytics_storage).isUndefined();
    assertThat(uetCalls[0][3].wait_for_update).isUndefined();
- name: Without ad_storage nothing is pushed - a partial payload would disable UET's own enforcement
  code: |-
    // Measured in bat.js: the first push sets consent.enabled = true and turns
    // shouldEnforce() off, while adStorageAllowed stays true because no
    // ad_storage arrived. A payload of the two signals UET ignores would
    // therefore switch off the EEA enforcement and leave the visitor granted.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'not_set', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'denied', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(0);
- name: An empty string from a variable is not a consent state
  code: |-
    // The signal SELECTs accept a GTM variable, so the value is whatever it
    // resolved to. In bat.js adStorageAllowed is ('denied' !== ad_storage), so
    // an empty string would read as granted.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: '', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(0);
- name: Clarity is all or nothing - consentv2 documents both fields as required
  code: |-
    let clarityCalls = [];
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) { clarityCalls.push([a, b]); };
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'denied', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'not_set', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    // UET still gets its push - ad_storage is configured.
    assertThat(uetCalls.length).isEqualTo(1);
    assertThat(uetCalls[0][3].ad_storage).isEqualTo('denied');
    // Clarity does not, because analytics_Storage would be missing.
    assertThat(clarityCalls.length).isEqualTo(0);
- name: Wait for Consent Update is passed on to UET as well
  code: |-
    // bat.js reads wait_for_update and caps it at 10000 ms. Without passing it
    // on, UET fires hits carrying the default state during the very window the
    // integrator asked it to wait.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true, cm_wait: '500',
      ad_storage: 'denied', ad_user_data: 'denied', ad_personalization: 'denied',
      analytics_storage: 'denied', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(1);
    assertThat(uetCalls[0][2]).isEqualTo('default');
    assertThat(uetCalls[0][3].wait_for_update).isEqualTo(500);
- name: Clarity is created through the queue when the global does not exist yet
  code: |-
    let created = [];
    let inWindow = [];
    let clarityCalls = [];
    let haveClarity = false;
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return haveClarity ? function(a, b) { clarityCalls.push([a, b]); } : undefined;
      return { hasResponse: true };
    });
    mock('setInWindow', function(key, val) { inWindow.push(key); haveClarity = true; return true; });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) { created.push(name); return function(a, b, c) {}; });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'granted', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(inWindow).contains('clarity');
    assertThat(created).contains('clarity.q');
    assertThat(clarityCalls.length).isEqualTo(1);
    assertThat(clarityCalls[0][0]).isEqualTo('consentv2');
- name: A value that is neither granted nor denied is not a consent state
  code: |-
    // The empty-string case alone did not pin this down: a GTM variable can
    // resolve to anything - 'yes', '1', a boolean. In bat.js adStorageAllowed
    // is ('denied' !== ad_storage), so every one of those reads as granted.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true,
      ad_storage: 'yes', ad_user_data: 'granted', ad_personalization: 'granted',
      analytics_storage: 'granted', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(0);
- name: The denied baseline covers all three signals even when only one is configured
  code: |-
    // A baseline built from the signals that happen to be set is not a
    // baseline. With only ad_storage configured, the other two must still be
    // denied first - otherwise they ride on UET's own default.
    let uetCalls = [];
    mock('copyFromWindow', function(key) {
      if (key === 'clarity') return function(a, b) {};
      return { hasResponse: true };
    });
    mock('setDefaultConsentState', function(o) {});
    mock('updateConsentState', function(o) {});
    mock('createQueue', function(name) {
      return function(a, b, c) { uetCalls.push([name, a, b, c]); };
    });
    runCode({
      ms_consent_mode: true, cm_update_after_default: true,
      ad_storage: 'granted', ad_user_data: 'not_set', ad_personalization: 'not_set',
      analytics_storage: 'not_set', personalization_storage: 'not_set',
      functionality_storage: 'not_set', security_storage: 'not_set'
    });
    assertThat(uetCalls.length).isEqualTo(2);
    assertThat(uetCalls[0][2]).isEqualTo('default');
    assertThat(uetCalls[0][3].ad_storage).isEqualTo('denied');
    assertThat(uetCalls[0][3].ad_user_data).isEqualTo('denied');
    assertThat(uetCalls[0][3].ad_personalization).isEqualTo('denied');
    assertThat(uetCalls[1][2]).isEqualTo('update');
    assertThat(uetCalls[1][3].ad_storage).isEqualTo('granted');
    assertThat(uetCalls[1][3].ad_user_data).isUndefined();
setup: ''


___NOTES___

# aGTM Custom Template

- Version 1.5
- Autor: Andi Petzoldt <andi@petzoldt.net>
- Last Update: 19.08.2026

## Description

This template sets the Google (and Microsoft) Consent Mode signals for GTM.

## Changes in 1.5

- Microsoft Consent Mode (`ms_consent_mode`) reworked and documented. Measured
  against the live `bat.bing.com/bat.js` (2026-08-19), not inferred from the
  Microsoft docs alone:
  - **`ad_storage` now decides whether anything is sent at all.** It is the only
    signal UET evaluates. The first consent push sets `uetConfig.consent.enabled`
    before the payload is even inspected, which turns off both UET's own
    EEA/UK/CH enforcement (where its default is DENIED) and `preEnforce()`,
    which otherwise reads Google Consent Mode out of `window.google_tag_data.ics`
    ~1.5 s after load. A payload without `ad_storage` would therefore disable
    that machinery AND leave the visitor on granted. When `ad_storage` is not
    configured, this tag sends nothing — the safer branch, not a blank one.
  - Only the literal values `granted` and `denied` count as configured. The
    signal fields accept a GTM variable; an empty or unexpected value used to be
    forwarded, and `adStorageAllowed` is computed as `'denied' !== ad_storage`,
    so anything else reads as granted.
  - The "Update after Default" baseline covers all three signals unconditionally.
    It used to be built from the signals that happened to be set, so `ad_storage`
    could be missing from the very default the mode exists for. UET has no region
    parameter, so that baseline is global.
  - `cm_wait` is passed on as UET's `wait_for_update` (UET caps it at 10 000 ms).
  - Clarity is all-or-nothing: Microsoft documents both `consentv2` fields as
    required, so the call is made only when `ad_storage` and `analytics_storage`
    are both configured.
  - The option was undocumented until now; see the README, including a section on
    when the checkbox is worth switching on at all.
- Tests: nine `___TESTS___` scenarios cover the Microsoft branch (no push without
  `ad_storage`, non-state values, all three signals, update mode, the complete
  denied baseline, `wait_for_update`, Clarity all-or-nothing, and the Clarity
  queue fallback).
- Cleanup: removed two dead `require()`s that the F-42 permission sweep had missed —
  `createQueue('dataLayer')` (used only in a commented-out line, superseded by
  `aGTM.f.fire`) and `makeTableMap` (never used at all).
- Permissions narrowed: with the dead `createQueue('dataLayer')` gone, the template no
  longer needs a direct read/write `access_globals` grant on the global `dataLayer`, so
  that entry was dropped. `write_data_layer` stays — that is the permission `gtagSet`
  requires for `url_passthrough` / `ads_data_redaction`, and its `keyPatterns` cover
  exactly those two keys.
- No behaviour change: the consent signals still go out through the
  `setDefaultConsentState` / `updateConsentState` template APIs.
- Note when re-importing: GTM will show the narrower permission set as a permission diff.

## Changes in 1.4

- Fix: guard against `aGTM.d.consent` being undefined (aGTM not loaded yet) so
  the tag no longer throws and skips setting the default consent state on the
  Consent Initialization trigger (fail-open closed).
- Fix: the consent-check fallback now runs in production (not only in debug) and
  re-reads the consent it just wrote.
- Fix: in "Update after Default" mode the region is now applied to the (denied)
  default via `setDefaultConsentState`; it stays off `updateConsentState`
  (which has no region parameter). A configured `cm_regions` is no longer ignored.
- New: optional `cm_grant_outside` — emit a global granted default (for the
  managed signals) so visitors outside the configured regions are granted by
  default (Google's two-default pattern).
- Tests: added `___TESTS___` regression scenarios (F-41) covering the fail-open
  guard, granted pass-through, update vs. default mode, region-scoped denied
  default in "update after default", the consent-check re-read, and the
  `not_set` signal omission.


